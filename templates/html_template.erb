<h1> Figure 1. Comparison of prevalent DEGs and CEGs in the different datasets (aldh18a1, ischaemia, sprouting angiogenesis (sprout) and sqle) </h1> 

<h2> Overlapping Prevalent DEGs </h2> 
	
<%= embed_pdf('plots/vennDiagram_DEG.pdf', 'width="600" height="600"') %>

<h2> Overlapping Prevalent CEGs </h2> 
	
<%= embed_pdf('plots/vennDiagram_CEG.pdf', 'width="600" height="600"') %>

<h2> Overlapping Prevalent DEGs and CEGs </h2> 
	
<%= embed_pdf('plots/vennDiagram_DEG_CEG.pdf', 'width="600" height="600"') %>

<h1> Figure 2. Overlap between DEG and CEG clusters and PPI modules. </h1> 

<%= embed_pdf('plots/overlap300.pdf', 'width="600" height="600"') %>
<%= embed_pdf('plots/overlap950.pdf', 'width="600" height="600"') %>

<h1> Tables. </h1> 

<h2> Table 1. Summary of the number of genes passing the counts per million (CPM) filter,
possible differential expressed genes (DEGs) and prevalent DEGs for the four different
datasets analysed. </h2>

<%= table(id: 'genes_expression_stats.txt', text: true, header: false) do |table|
		table.unshift(['Dataset', 'Expressed genes (after CPM filter)', 'Possible DEG', 'Prevalent DEG'])
	end
%>

<h2> Table 4. Total number of clusters, maximum size and median size for full (300) and
confident (conf., 950) PPI networks for the clustering methods. </h2>

<%= table(id: 'all_methods_stats.txt', text: true, header: true) do |table|
	
	end
%>

<h2> Table 5. Assignment of genes SEMA3D, IL33, SQLE, ALDH18A1 and VEGFA to
STRING clusters. </h2>

<%= table(id: 'output_matches.txt', text: true, header: false) do |table|
		table.unshift(['Network', 'Study gene', 'Cluster ID', 'Total genes by cluster'])
	
	end
%>

<h2> Table 6. Overlap between clusters of the the PPIs network and DEG lists. The first two
columns show the STRING cluster ID obtained from using the combined score cut-off of
950 (rber_pots) and its size. </h2>

<%= table(id: 'degs_table.txt', text: true, header: false) do |table|
		table.unshift(['ID', 'Size', 'Gene', 'ALDH18A1', 'Ischaemia', 'Sprout', 'SQLE'])
	
	end
%>

<h2> Table 7. Overlap between clusters of the the PPIs network and CEG lists. The first two
columns show the STRING cluster ID obtained from using the combined score cut-off of
950 (rber_pots) and its size. </h2>

<%= table(id: 'cegs_table.txt', text: true, header: false) do |table|
		table.unshift(['ID', 'Size', 'Gene', 'ALDH18A1', 'Ischaemia', 'Sprout', 'SQLE'])
	
	end
%>